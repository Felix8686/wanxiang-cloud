{
  "name": "wanxiang-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply wanxiang-cloud-dev --local",
    "db:migrate:remote": "wrangler d1 migrations apply wanxiang-cloud-dev --remote"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.34.0"
  }
}
